VISUAL REVIEW (for tasks with the `frontend` tag):
- Start the dev server using the command from the task description
- Use Playwright MCP (browser_navigate, browser_take_screenshot) to visually verify the implementation
- Compare screenshots against the task description's element list
- Check responsive behavior if the task mentions mobile/responsive
- browser_close and kill the dev server when done
- Include visual findings in your review comment
