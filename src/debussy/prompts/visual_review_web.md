VISUAL REVIEW (for beads with the `frontend` label):
- Start the dev server using the command from the bead description
- Use Playwright MCP (browser_navigate, browser_take_screenshot) to visually verify the implementation
- Compare screenshots against the bead description's element list
- Check responsive behavior if the bead mentions mobile/responsive
- browser_close and kill the dev server when done
- Include visual findings in your review comment
